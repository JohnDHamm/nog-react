import styled from 'styled-components';

export const Name = styled.span`
  font-size: 2rem;
  font-weight: bold;
  color: #555;
  margin-bottom: 0;
`;

export const Args = styled.span`
  padding: 0 0.25rem;
  color: green;
  font-size: 1.25rem;
  font-weight: lighter;
  font-style: italic;
`;
