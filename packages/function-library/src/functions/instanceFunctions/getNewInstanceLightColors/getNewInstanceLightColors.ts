const getNewInstanceLightColors = (): Array<number> => {
  const newLightColors = [];
  for (let i = 0; i < 30; i++) {
    newLightColors.push(7);
  }
  return newLightColors;
};

export default getNewInstanceLightColors;
